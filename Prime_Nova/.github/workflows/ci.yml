name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: ./Prime_Nova

      - name: Type check
        run: pnpm type-check
        working-directory: ./Prime_Nova

      - name: Lint
        run: pnpm lint
        working-directory: ./Prime_Nova

      - name: Format check
        run: pnpm format:check
        working-directory: ./Prime_Nova

      - name: Run tests
        run: pnpm test
        working-directory: ./Prime_Nova

      - name: Build application
        run: pnpm build
        working-directory: ./Prime_Nova

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: Prime_Nova/.next/
          retention-days: 1

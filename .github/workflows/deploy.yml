name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: |
          npm run build
          touch out/.nojekyll
          
          # Create a simple 404.html for GitHub Pages
          echo '<!DOCTYPE html><html><head><meta http-equiv="refresh" content="0; url=/Prime-Nova/404.html" /></head><body>Redirecting...</body></html>' > out/404.html
          
          # Copy CNAME if it exists
          if [ -f CNAME ]; then
            cp CNAME out/
          fi

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: out
          branch: gh-pages
          clean: true
          clean-exclude: |
            .nojekyll
            CNAME
          commit-message: "Deploy to GitHub Pages"
          force: true
